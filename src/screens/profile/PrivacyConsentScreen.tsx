import React from 'react'; import { Platform, StyleSheet, Text } from 'react-native'; import { NativeStackScreenProps } from '@react-navigation/native-stack';
import { Screen } from '../../components/Screen'; import { ProfileHeader, ProfileRow, ProfileSection } from '../../components/ProfileUi'; import { RootStackParamList } from '../../navigation/types'; import { useAppContext } from '../../state/AppContext'; import { getThemeColors, typography } from '../../design/tokens';
type Props=NativeStackScreenProps<RootStackParamList,'PrivacyConsent'>;
export const PrivacyConsentScreen=({navigation}:Props)=>{const {themeMode,onboarding}=useAppContext();const palette=getThemeColors(themeMode);return <Screen scroll><ProfileHeader navigation={navigation} title="Privacy & Consent"/><Text style={[styles.copy,{color:palette.textMuted}]}>You control which health information Fiteatsy can use.</Text><ProfileSection><ProfileRow icon="heart-outline" color="#2FD36B" title="Health Data Access" subtitle="Review platform permissions and synchronization" onPress={()=>navigation.navigate('ConnectedHealth')}/><ProfileRow icon="phone-portrait-outline" color="#35B8D0" title={Platform.OS==='ios'?'Apple Health':'Health Connect'} subtitle="Read-only access is governed by your device" onPress={()=>navigation.navigate('ConnectedHealth')}/><ProfileRow icon="people-outline" color="#FF9F0A" title="Consultant Access" subtitle={onboarding?.shareMeasurementsWithConsultant?'Health measurements sharing enabled':'Health measurements sharing not enabled'}/></ProfileSection><ProfileSection label="Your information"><ProfileRow icon="document-text-outline" color="#8E8E93" title="Privacy policy" subtitle="Legal content is unavailable until the governed public URL is configured"/><ProfileRow icon="reader-outline" color="#8E8E93" title="Terms & conditions" subtitle="Legal content is unavailable until the governed public URL is configured"/></ProfileSection></Screen>};
const styles=StyleSheet.create({copy:{...typography.body,marginBottom:18}});
