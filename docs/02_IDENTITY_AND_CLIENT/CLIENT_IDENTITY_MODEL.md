# Client Identity Model

Rules:
- Email is not the permanent identity.
- Mobile is not the permanent identity.
- Name is never an identity key.
- users.id is not the public client contract.
- CAP-001 correlation remains separate.
