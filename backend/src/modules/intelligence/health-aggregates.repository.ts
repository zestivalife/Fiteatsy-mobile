import crypto from 'node:crypto';import {pool} from '../../db/pool.js';import type {ClientOwnershipContext} from '../platform/platform.types.js';import type {DailyAggregate} from './health-aggregation-v1.js';import {HEALTH_INTELLIGENCE_VERSION} from './health-intelligence-v1.js';
export const replaceDailyAggregates=async(owner:ClientOwnershipContext,rows:DailyAggregate[])=>{const client=await pool.connect();try{await client.query('begin');for(const r of rows)await client.query(`insert into daily_health_aggregates(id,user_id,client_id,aggregate_date,metric_type,aggregation_method,value,unit,source_observation_ids,source_count,freshness,calculation_version) values($1,$2,$3,$4,$5,$6,$7,$8,$9::jsonb,$10,$11,$12) on conflict(client_id,aggregate_date,metric_type,calculation_version) do update set aggregation_method=excluded.aggregation_method,value=excluded.value,unit=excluded.unit,source_observation_ids=excluded.source_observation_ids,source_count=excluded.source_count,freshness=excluded.freshness,calculated_at=now()`,[`hagg_${crypto.randomUUID()}`,owner.accountId,owner.clientId,r.date,r.metric,r.method,r.value,r.unit,JSON.stringify(r.observationIds),r.observationIds.length,r.freshness,HEALTH_INTELLIGENCE_VERSION]);await client.query('commit');}catch(e){await client.query('rollback');throw e;}finally{client.release();}};
export const listDailyAggregates=async(owner:ClientOwnershipContext,days=28)=>{const r=await pool.query(`select aggregate_date::text as date,metric_type as metric,value::float8 as value,unit,aggregation_method as method,source_observation_ids as "observationIds",freshness from daily_health_aggregates where user_id=$1 and client_id=$2 and aggregate_date>=current_date-$3::int order by aggregate_date desc`,[owner.accountId,owner.clientId,days]);return r.rows as DailyAggregate[];};
