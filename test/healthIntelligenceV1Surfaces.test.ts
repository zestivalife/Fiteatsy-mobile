import fs from 'node:fs';import path from 'node:path';const root=path.resolve(__dirname,'..');const read=(p:string)=>fs.readFileSync(path.join(root,p),'utf8');
describe('Health Intelligence V1 canonical surfaces',()=>{test('Star Orb uses the approved framework names and direct canonical fields',()=>{const home=read('src/screens/home/HomeScreen.tsx');expect(home).toContain('healthSummary?.healthIntelligenceScore');expect(home).toContain('healthSummary?.recoveryScore');expect(home).toContain('healthSummary?.activityScore');expect(home).toContain('healthSummary?.sleepScore');expect(home).toContain('healthSummary?.nutritionScore');expect(home).toContain('healthSummary?.calmScore');expect(home).toContain('healthSummary?.cycleScore');expect(home).toContain("label: 'Nourishment'");expect(home).not.toContain("label: 'Active Performance'");expect(home).not.toContain("label: 'Physical Wellness'");});test('Tracker consumes canonical histories without manufacturing or duplicating scores',()=>{const tracker=read('src/screens/home/TrackerScreen.tsx');expect(tracker).toContain('getHealthIntelligenceV1');expect(tracker).toContain('getHealthScoreSummary');expect(tracker).toContain('getHealthScoreHistory()');expect(tracker).toContain('Promise.allSettled');expect(tracker).toContain("scoreSeries('recovery')");expect(tracker).toContain("observationSeries(['resting_heart_rate', 'heart_rate'])");expect(tracker).not.toMatch(/trendValues7d\.map/);expect(tracker).not.toMatch(/Math\.round\(v\s*[-+]\s*\d/);expect(tracker).toContain("['Energy Balance', masterScoreSummary?.energyBalanceScore");expect(tracker).toContain("['Body & Biomarker Health', masterScoreSummary?.bodySupportScore");expect(tracker).not.toContain('Physical Ease');expect(tracker).not.toContain("['Active Performance', masterScoreSummary?.activePerformanceScore");expect(tracker).not.toContain("['Nourishment', masterScoreSummary?.nourishmentScore");expect(tracker).not.toContain('const stepTarget = 5000');expect(tracker).not.toContain("renderMetricRow('Cardio Efficiency'");expect((tracker.match(/renderMetricRow\('Recovery Signal'/g)??[])).toHaveLength(1);});test('backend exposes one versioned projection for client and consultant use',()=>{const routes=read('backend/src/modules/intelligence/intelligence.routes.ts');const consultant=read('backend/src/modules/consultants/consultants.service.ts');expect(routes).toContain("intelligenceRouter.get('/v1'");expect(consultant).toContain("calculationVersion: 'HEALTH_INTELLIGENCE_V1'");expect(consultant).toContain("scoreType === 'health_intelligence'");});});
