import fs from 'node:fs';import path from 'node:path';const root=path.resolve(__dirname,'..');const read=(p:string)=>fs.readFileSync(path.join(root,p),'utf8');
describe('Health Intelligence V1 canonical surfaces',()=>{test('Star Orb uses canonical activity/calm and leaves Mind pending',()=>{const home=read('src/screens/home/HomeScreen.tsx');expect(home).toContain('healthSummary?.activityScore');expect(home).toContain('healthSummary?.calmScore');expect(home).toContain("key: 'mind'");expect(home).toMatch(/key: 'mind',[\s\S]*?score: null/);expect(home).not.toContain('activePerformanceScore ?? healthSummary?.activityScore');});test('Tracker consumes backend V1 and has no UI-local targets or Cardio Efficiency score',()=>{const tracker=read('src/screens/home/TrackerScreen.tsx');expect(tracker).toContain('getHealthIntelligenceV1');expect(tracker).toContain('canonicalIntelligence?.scores.activity.score');expect(tracker).toContain('canonicalIntelligence?.scores.sleep.score');expect(tracker).not.toContain('const stepTarget = 5000');expect(tracker).not.toContain("renderMetricRow('Cardio Efficiency'");});test('backend exposes one versioned projection for client and consultant use',()=>{const routes=read('backend/src/modules/intelligence/intelligence.routes.ts');const consultant=read('backend/src/modules/consultants/consultants.service.ts');expect(routes).toContain("intelligenceRouter.get('/v1'");expect(consultant).toContain("calculationVersion: 'HEALTH_INTELLIGENCE_V1'");expect(consultant).toContain("scoreType === 'health_intelligence'");});});
